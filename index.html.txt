<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KiraSTORE - Zapatillas y Ropa Deportiva</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; background: #111; color: #fff; padding-right: 300px; }
        header { background: #000; padding: 20px; text-align: center; }
        h1 { margin: 0; color: #00c853; }
        .container { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 20px; padding: 20px; }
        .card { background: #1c1c1c; padding: 15px; border-radius: 10px; text-align: center; border: 1px solid #333; }
        .card img { width: 100%; height: 200px; object-fit: cover; border-radius: 10px; }
        button { background: #00c853; color: #fff; border: none; padding: 10px 15px; border-radius: 5px; cursor: pointer; font-weight: bold; }
        button:hover { background: #00a844; }
        
        /* Carrito Lateral */
        #cart { position: fixed; right: 0; top: 0; background: #000; width: 300px; height: 100%; padding: 20px; overflow-y: auto; border-left: 2px solid #00c853; box-sizing: border-box; }
        #cart h2 { text-align: center; border-bottom: 1px solid #00c853; padding-bottom: 10px; }
        .cart-item { border-bottom: 1px solid #333; padding: 10px 0; font-size: 14px; display: flex; justify-content: space-between; }
        .total { font-size: 20px; margin-top: 20px; font-weight: bold; text-align: center; color: #00c853; }

        /* Responsivo para celulares */
        @media (max-width: 768px) {
            body { padding-right: 0; padding-bottom: 300px; }
            #cart { width: 100%; height: 300px; top: auto; bottom: 0; border-left: none; border-top: 2px solid #00c853; }
        }
    </style>
</head>
<body>

<header>
    <h1>KiraSTORE</h1>
    <p>Zapatillas y Ropa Deportiva</p>
</header>

<div class="container">
    <div class="card">
        <img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff" alt="Nike">
        <h3>Nike Air Force 1</h3>
        <p>$120.000</p>
        <button onclick="addToCart('Nike Air Force 1', 120000)">Agregar al carrito</button>
    </div>

    <div class="card">
        <img src="https://images.unsplash.com/photo-1600185365483-26d7a4cc7519" alt="Adidas">
        <h3>Adidas Superstar</h3>
        <p>$110.000</p>
        <button onclick="addToCart('Adidas Superstar', 110000)">Agregar al carrito</button>
    </div>

    <div class="card">
        <img src="https://images.unsplash.com/photo-1595950653106-6c9ebd614d3a" alt="Puma">
        <h3>Puma RS-X</h3>
        <p>$100.000</p>
        <button onclick="addToCart('Puma RS-X', 100000)">Agregar al carrito</button>
    </div>
</div>

<div id="cart">
    <h2>Tu Carrito</h2>
    <div id="cart-items"></div>
    <div class="total">Total: $<span id="total">0</span></div>
    <button onclick="finalizarCompra()" style="width:100%; margin-top:20px;">Finalizar por WhatsApp</button>
</div>

<script>
    let cart = [];

    function addToCart(name, price) {
        cart.push({ name, price });
        renderCart();
    }

    function renderCart() {
        const cartItems = document.getElementById('cart-items');
        cartItems.innerHTML = '';
        let total = 0;

        cart.forEach(item => {
            const div = document.createElement('div');
            div.className = 'cart-item';
            div.innerHTML = `<span>${item.name}</span> <span>$${item.price}</span>`;
            cartItems.appendChild(div);
            total += item.price;
        });

        document.getElementById('total').textContent = total.toLocaleString();
    }

    function finalizarCompra() {
        if (cart.length === 0) return alert("El carrito está vacío");
        
        let mensaje = "Hola KiraSTORE! Quiero comprar:%0A";
        cart.forEach(item => {
            mensaje += `- ${item.name} ($${item.price})%0A`;
        });
        mensaje += "%0A*Total: $" + document.getElementById("total").innerText + "*";
        
        // RECUERDA: Cambia el número 5491100000000 por el tuyo real
        window.open(`https://wa.me/5491100000000?text=${mensaje}`);
    }
</script>

</body>
</html>
